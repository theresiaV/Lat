class numberOfWord
{
	public static void main (String[] args)
	{ String sentence = " The quick brown fox jumps over the lazy dog";
	  int count = 0;
	  
	  if (!("".equals(count.substring(0,1)))||!("".equals(count.substring(count.length()-1))))
	  {
		  for (int i = 0;i<count.length();i++)
		  { if (count.charAt(i) = ' ')
			  {
				  count++;
			  }
			count += 1;
		  }
	  }
	  System.out.print("Number of String: " + count);
	}
}